function App() {
  return (
    <div className="min-h-screen bg-zinc-50 text-zinc-900">
      <main className="mx-auto max-w-3xl p-6">
        <h1 className="text-2xl font-semibold">Agentic Workflow Builder</h1>
        <p className="mt-2 text-zinc-600">
          Replace this with the canvas — wire up{' '}
          <code className="rounded bg-zinc-200 px-1 py-0.5 text-sm">@xyflow/react</code> here.
        </p>
      </main>
    </div>
  )
}

export default App
